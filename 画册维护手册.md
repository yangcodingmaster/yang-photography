# 画册维护手册（给作者自己看）

> 这份手册讲的是 **画册式阅读**（zine 系列文件）怎么维护：加照片、删照片、加系列、调版式。
> 它假设你不写代码——每一步都告诉你打开哪个文件、改哪一行、跑哪条命令。
> （网站其他部分的维护，看 CLAUDE.md 里的"内容维护指南"，那套完全没变。）

---

## 一、这套东西是什么

一句话：**点开任何一个本子，不再是"网格 / 弹窗翻页"，而是一本往下滚的画册**——
每屏一张照片，滑一下翻一页，文字按你在 data.js 里写的内容自动找位置。

**这已经是正式体验**（2026-07-30）：**Gallery 的系列、Archive 的每一年、Film 的每一卷，
点开都是画册**，三个板块共用同一个模板文件。

| 网址 | 是什么 | 从哪进 |
|---|---|---|
| `zine.html?id=系列ID` | Gallery 一个系列 | 作品集页点卡片 |
| `zine.html?year=2025` | Archive 一年的散片 | Archive 页点开那本书 |
| `zine.html?film=卷ID` | Film 一卷（照片嵌在片框里，齿孔和片边字都在） | Film 页点开那个筒 |
| `zine-index.html` | 画册目录：自动列出全部 Gallery 系列（预览用，未接导航） | 直接输网址 |

**每一本都有自己的网址了**——想单独分享某一卷、某一年，把地址栏里的网址发出去就行
（微信卡片目前还是全站通用的那张，见教程 5）。

---

## 二、各文件的作用（谁能删、谁不能碰）

### 画册体系的文件

| 文件 | 作用 | 能不能动 |
|---|---|---|
| `zine.html` | ⭐ **画册模板本体**，唯一需要长期维护的画册文件。Gallery / Archive / Film 三个板块全部共用这一个，靠 `?id=` `?year=` `?film=` 区分 | 调版式开关在这里改（见第六节） |
| `zine-dims.js` | 图片宽高表（照片加载时页面不跳版）。**脚本生成物，别手改** | 加了照片后跑一次脚本重新生成 |
| `scripts/make-zine-dims.py` | 生成上面那个宽高表的脚本 | 不用改，只管跑 |
| `zine-index.html` | 画册目录页（自动列出 Gallery 全部系列） | 预览用；不需要时可删 |

> 比稿期做过七个探索原型（Gallery 编辑式/纯净式/分章式/满屏式/图文样本册五个、
> Film 垂直胶片条、Archive 年册），定稿后已从网站里删除——**它们都在 git 提交历史里**。

### 与老系统的关系（重要）

| 文件 | 现状 |
|---|---|
| `gallery.html` | 改了一行：系列卡片的链接指向 zine.html（这一行也是回退开关） |
| `archive.html` | 书架**原样保留**；只把"翻开一本书"从"页内阅读视图"改成"跳画册页"。书翻开的动画特意留着 |
| `film.html` | 胶卷柜**原样保留**；只把"翻开一卷"改成"跳画册页"。片框（齿孔、片边字）跟着搬进画册了 |
| `series.html`，以及 archive/film 里的**页内阅读视图代码** | 三处旧阅读方式**已退役但保留**：没有任何入口链到它们，是随时可回退的后路 |
| `fluid.js`、`index.html`、`data.js` | 一个字没动。fluid.js 还被那三处退役代码引着，**现在不能删** |
| `data.js` | 仍是全站唯一数据源。画册读的也是它——你以后**填文案只填一处**，新旧页面同时生效 |

---

## 三、教程 1：往现有系列里加照片

以往 `goodbye-ucl` 加一张为例，共四步：

1. **放图**：把压缩好的 JPEG（长边 2560/质量 90，老规矩）放进
   `assets/images/gallery/goodbye-ucl/`，按顺序起名（比如已有 16 张就叫 `17.jpeg`）
2. **登记**：打开 `data.js`，找到 `'goodbye-ucl': [ ... ]`，照抄上一张的格式在想要的位置加一段：

   ```javascript
   {
     src:      'assets/images/gallery/goodbye-ucl/17.jpeg',
     alt:      '',
     caption:  '',        // 想好了再填，空着就是纯照片页
     date:     '',
     location: '',
     desc:     '',
     meta:     '',
   },
   ```

   > 数组里的位置 = 画册里的页序。插在中间就是中间的一页。

3. **跑两条命令**（顺序执行，各一两秒）：

   ```bash
   python3 scripts/make-small-images.py
   ```

   ```bash
   python3 scripts/make-zine-dims.py
   ```

4. **看效果**：刷新 `zine.html?id=goodbye-ucl`（浏览器缓存 data.js，用 Cmd+Shift+R 硬刷新）。
   页码、版权页张数会自动跟着变，不用手改。

## 四、教程 2：删照片

1. 打开 `data.js`，把那张照片的整段条目（从 `{` 到 `},`）删掉——**这一步就够了**，
   画册和老页面同时生效，页码自动重排
2. 可选的收尾：`assets/images/` 和 `assets/images-sm/` 里对应的图片文件想删就删
   （不删也不影响任何页面，只占一点磁盘）；尺寸表里多出来的条目无害，
   下次跑 `make-zine-dims.py` 时自动消失

## 五、教程 3：添加一个新系列

和以前完全一样，**画册不需要任何额外步骤**：

1. 照片文件夹丢进 `assets/images/gallery/`（文件夹名 = 系列 ID，连字符）
2. 跟 Claude 说「加一个系列，放 XX 旁边，叫 XX」触发 `add-gallery-series` skill
   （它会自动转码压缩、写入 data.js；末尾自动补跑小图脚本）
3. 多跑一条 `python3 scripts/make-zine-dims.py`
4. 完成。`zine.html?id=新系列ID` 立刻能看——模板认得所有写进 allSeries 的系列，
   分章系列（type: 'sectioned'）也自动处理（每章插一页章节扉页）

## 五点五、Archive 和 Film 怎么加东西

**流程跟以前一模一样**（见 CLAUDE.md 里"如何往 Archive 加照片 / 如何往 Film 加一卷"），
画册不需要任何额外步骤，只是记得跑那两条脚本：

```bash
python3 scripts/make-small-images.py && python3 scripts/make-zine-dims.py
```

- **Archive**：往 `allArchive['2025']` 数组加一行就行。加新一年（比如 `'2026': []`）时，
  书架自动多一本书，`zine.html?year=2026` 也立刻能用
- **Film**：往 `allFilms` 加一卷，胶卷柜自动多一个筒，`zine.html?film=新卷ID` 立刻能看。
  ⚠️ 新型号记得在 film.html 的 `SHELL_STYLES` 补一行配色（这是筒身的事，跟画册无关）

**两个板块在画册里的样子**：

| 板块 | 封面 | 每一页 |
|---|---|---|
| Archive | 巨大的年份数字 + "散片档案 · 二十九张" | 满屏一张 + 右下角页码；写了 `note` 就显示在页脚左边 |
| Film | 型号（有 `title` 的话作品名在上）+ 参数行 + `desc` 导语 | 照片嵌在**片框**里（135 齿孔 + 片边字 / 120 素黑边），写了 `note` 显示在页脚 |

## 六、教程 4：自定义版式（画册的"排版语言"）

> 这一节讲的是 **Gallery 系列**的排版自由度。Archive 走固定的极简版式
> （它的定位就是"只放照片"），Film 走固定的片框版式（那是胶片的身份）。

### 每张照片的文字模式：自动规则

不写任何配置时，模板看这张照片在 data.js 里**有什么文字**来定版式：

| 照片有什么 | 自动版式 |
|---|---|
| `desc`（长描述） | 照片收窄，右侧出文字面板（作品名/日期地点/描述/器材） |
| `note`（一句话） | 页边批注：文字缩到页边角落 |
| 只有 `caption` | 图下注：底部一行小字 |
| 什么都没有 | 纯照片页，只有页码 |

### 逐张覆盖：layout 字段

哪张想指定版式，就在它的条目里加一行：

```javascript
layout: 'panel-left',   // 可选值：panel-right / panel-left / margin / caption / plain
```

典型用途：照片主体压在右边、文字放右会打架 → 这张写 `panel-left`。

### 插页：文字页和空白页

在 photos 数组的任意位置插一条（**不占页码**）：

```javascript
{ text: '翻到一半，停一口气。' },   // 独立文字页：一整屏只放这几句
{ blank: true },                    // 空白页：一整屏纯纸面
```

### 全局开关（在 zine.html 里，搜变量名就能找到）

| 开关 | 默认 | 改成什么 |
|---|---|---|
| `PANEL_SIDE` | `'right'` 文字面板固定在右 | `'left'` 固定在左；`'alternate'` 左右交替 |
| `SETTLE_SCALE` | `true` 照片浮现带落定感 | `false` 只上浮不缩放 |

---

## 六点五、教程 5：自定义微信分享卡片（OG）

把网址发到微信，对方看到的卡片长这样：**左边一张小方图 + 右边一行标题 + 标题下一行小字**。
这三样分别由每个 HTML 文件 `<head>` 里的三行决定（每个页面都有同一组，改谁的卡片就打开谁）：

```html
<meta property="og:title"       content="作品集 · 赵洋摄影" />        ← 卡片标题
<meta property="og:description" content="八个系列：从虚焦……" />      ← 标题下的小字
<meta property="og:image"       content="https://…/share-card.jpg" /> ← 左侧小方图
```

改法和注意事项：

1. **标题**：改 `og:title` 的 content 就行，建议 12 字以内（太长微信会截断）
2. **描述**：改 `og:description` 的 content。⚠️ 手机端只显示约 **15–20 个字**，
   写长了后面全是白写；同一句话建议同步改同文件里的 `<meta name="description">`（给搜索引擎看的）
3. **小方图**：所有页面共用 `assets/images/share/share-card.jpg`。
   想换里面那张照片：改 `scripts/make-share-card.sh` 里的 `PHOTO` 一行，
   跑 `bash scripts/make-share-card.sh`（详见 CLAUDE.md"分享图怎么改"）
4. **改完不生效？** 微信会缓存抓取结果——把发出去的网址后面加个 `?v=2` 强制重抓
5. **两条不能踩的线**：og:image 和 og:url 必须是带 `https://` 域名的完整网址
   （微信服务器拿它去下载图，相对路径抓不到）；卡片内容必须写死在 HTML 里
   （微信不执行 JS）——所以 `zine.html?id=xxx` 八个系列共用一张通用卡片，
   **做不到每个系列各自的封面和标题**。想做到需要"每系列一个静态页"方案，
   已列入 CLAUDE.md"未来可能的方向"，要做时跟 Claude 说一声

---

## 七、旧文件去留 & 后悔药

**要不要删旧文件？现在都别删。**

- 三处旧阅读方式（series.html 整个文件、archive.html 和 film.html 里的页内阅读视图代码）
  已经没有入口了，但留着就是后路。用稳一阵子再说
- `fluid.js` 还被那三处代码引着，**现在不能删**；等哪天清理了它们，它才能一起走
- 比稿期的探索原型已经删掉了（不随网站上线）——**删掉不等于丢**：
  每一版都在 git 提交历史里，随时找得回来

**如果画册这个方向最终不行，怎么回退？**

- 三个板块各有一个回退点，都很小：
  - Gallery：gallery.html 里卡片的链接改回 `series.html?id=`
  - Archive：archive.html 的 `openBook()` 里，把跳转那行换回原来的"打开阅读视图"
  - Film：film.html 的 `openRoll()` 同理
  （改哪里代码注释里都写着；zine 文件删不删都行，没有链接就等于不存在）
- 如果已经合并了 PR，在 GitHub 上点那个 PR 里的 **Revert** 按钮就能一键生成反向提交
- 腾讯云正式服务器只在你跑 `bash scripts/deploy.sh` 时才会更新——
  不跑它，线上就永远是改版前的样子。这是最硬的一道保险
